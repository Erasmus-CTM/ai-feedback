---
title: "Working feedback examples"
filters:
  - ai-feedback
  - pyodide-interaktiv
  - math-exercise
  - py-exercise
---

This page is both a demonstration for course authors and a set of activities you
can try as a learner. Each **For course authors** note explains the feature being
demonstrated. In Non-Python activities 1–6, the bordered **Your task** card contains the learner's instructions,
response field and Feedback button. Author notes are not sent for feedback.
Reviewer guidance is configured separately from the learner's task.

Choose a tab below. All examples live in this one document.

:::::: {.panel-tabset}

## Non-Python {#non-python}

### How to try activities 1–6

1. Read **Your task**, then edit the sample response or enter your own. Some
   sample responses contain deliberate beginner mistakes.
2. Choose **Feedback**. On a first visit, **Copy prompt** mode shows a prompt to
   paste into your chosen AI chat.
3. For a reply on this page, open the **⚙ Feedback settings** cogwheel, select
   **Direct API**, and enter your institution's base URL, model and personal key.
   Their cogwheels open the same settings, shared across this website. Saved
   settings are reused on later visits.

For image activities, choose a model that accepts images. In **Copy prompt** mode,
attach the original image separately in your AI chat. In **Direct API** mode, your
response, selected source/context and uploaded images go to the configured
provider. Images are kept in memory, not saved in browser storage.

### 1. Beginner Spanish writing

::: {.callout-note .example-author-notes}
### For course authors

**Feature:** a plain textarea activity, without a checker or Python runtime.
The `language-quality` profile reviews Spanish at the author-defined level
"Spanish course 1" and explains improvements in English. The starting text
contains verb-agreement mistakes; learners may edit it or write their own text.
`context="none"` keeps these demonstration notes out of the prompt.
:::

::: {.ai-feedback #spanish-writing profile="language-quality" response-language="es" feedback-language="en" learner-level="Spanish course 1" context="none"}
**Your task**

Write three to five simple Spanish sentences about where you live and what you
do in the morning. Use the draft below as a starting point, or replace it with
your own text. Choose **Feedback** when you are ready for advice in English.

::: {.feedback-criteria}
Review vocabulary and present-tense grammar appropriate to Spanish
course 1.
:::

::: {.feedback-criteria}
Suggest up to three useful improvements while preserving the learner's
meaning and simple style; do not replace the whole response.
:::

::: {.feedback-starter}
Yo vive en Trondheim. Cada mañana yo come pan y bebo café.
:::
:::

### 2. English to Spanish translation

::: {.callout-note .example-author-notes}
### For course authors

**Feature:** an original text nested inside the activity as `.feedback-source`.
The `translation` profile receives the English source separately from the
Spanish response and gives feedback in English. The starting translation is
valid: try changing a detail to see how meaning and grammar are reviewed.
:::

::: {.ai-feedback #translation profile="translation" source-language="en" response-language="es" feedback-language="en" learner-level="Spanish course 1" context="none"}
**Your task**

Translate the English text below into Spanish using simple language. Review the
draft in the response field, or replace it with your own translation, then
choose **Feedback** for advice in English.

::: {.feedback-source}
Every morning, Maria walks to school with her brother. They like studying languages.
:::

::: {.feedback-starter}
Cada mañana, María camina a la escuela con su hermano. Les gusta estudiar idiomas.
:::
:::

### 3. Spanish adjective agreement

::: {.callout-note .example-author-notes}
### For course authors

**Feature:** a reusable lesson block selected with `context="agreement"`.
The legacy `.math-exercise-context` class works for language learning too;
`.ai-feedback-context` and `.ai-context` are aliases. Only the tagged lesson is
included as context. The interface stays in English, while feedback is in
Norwegian Bokmål.
:::

**Lesson reminder**

::: {#agreement .math-exercise-context}
An adjective agrees in gender and number with the noun it describes.
:::

::: {.ai-feedback #context-alias profile="language-quality" response-language="es" feedback-language="nb" learner-level="Spanish course 1" context="agreement"}
**Your task**

Write one Spanish sentence describing two red houses. Use the lesson reminder
above to check your adjective endings. Edit the draft, then choose **Feedback**;
the explanation will be in Norwegian Bokmål.

::: {.feedback-starter}
Las casas son rojo.
:::
:::

### 4. Feedback on handwritten Spanish

::: {.callout-note .example-author-notes}
### For course authors

**Feature:** `image-upload="true" image-role="response"` treats an uploaded image
as the learner's work, so typed text is optional. Use a vision-capable model for
direct feedback. The downloadable, AI-generated handwriting sample contains
two deliberate beginner mistakes for testing; feedback wording will vary by model.

To test image reading, leave **Your response** empty. Look for feedback on the
verb forms in the second and third sentences. The sample is not attached automatically.
:::

::: {.ai-feedback #handwriting profile="language-quality" response-language="es" feedback-language="en" learner-level="Spanish course 1" image-upload="true" image-role="response" context="none"}
**Your task**

Write three short Spanish sentences introducing yourself, saying where you live,
and mentioning something you like. Photograph your handwriting and select the
photo with **Upload images**, or use the sample below to try the activity.
You may leave **Your response** empty. Choose **Feedback** for advice in English.

[Download the Spanish handwriting sample (PNG)](assets/spanish-handwriting.png){#handwriting-sample-download download="spanish-handwriting.png"}
Save the image, then select it with **Upload images**.

::: {.feedback-criteria}
Read the handwritten Spanish and identify up to three useful
improvements to vocabulary, grammar or spelling at Spanish course 1 level. If a
word is unreadable, say so instead of guessing. Do not rewrite the whole response.
:::
:::

### 5. Describe a picture in Spanish

::: {.callout-note .example-author-notes}
### For course authors

**Feature:** `image-role="source"` makes the picture supporting material, while
the typed Spanish description is the response being reviewed. Unlike the
handwriting activity, an image alone is not a learner response. Try a photo of a
room or a landscape and use a vision-capable model for direct feedback.
:::

::: {.ai-feedback #describe-picture profile="language-quality" response-language="es" feedback-language="en" learner-level="Spanish course 1" image-upload="true" image-role="source" context="none"}
**Your task**

Upload a picture of a room or a landscape. In **Your response**, write three simple
Spanish sentences describing things you can see in it. Choose **Feedback** for
advice in English about your Spanish and how well the description fits the picture.

::: {.feedback-criteria}
Review vocabulary and grammar appropriate to Spanish course 1, and
whether the description matches the visible picture. Do not invent details that
cannot be seen, and do not supply a complete replacement description.
:::
:::

### 6. German to Norwegian on an English page

::: {.callout-note .example-author-notes}
### For course authors

**Feature:** independent source, response and feedback languages. This page and
its interface are English; the original is German, the response is Norwegian
Bokmål, and the feedback is English. The reusable source block is marked
`.ai-context`. The activity gives it its role with `source="german-original"`.
`context="none"` excludes these notes without removing the explicit source.
The draft includes an unnatural time expression for the learner to improve.
:::

**Original text — German**

::: {#german-original .ai-context}
Am Samstag fährt Lena mit dem Zug nach Oslo. Sie besucht ihre Schwester,
die seit zwei Jahren dort wohnt. Wenn das Wetter gut ist, gehen sie am
Nachmittag am Hafen spazieren. Am Sonntag fährt Lena wieder nach Hause.
:::

::: {.ai-feedback #german-to-norwegian profile="translation" source="german-original" source-language="de" response-language="nb" feedback-language="en" context="none"}
**Your task**

Translate the German passage above into Norwegian Bokmål. Edit the draft below,
or replace it with your own translation. Choose **Feedback** for advice in
English about meaning, natural Norwegian phrasing and grammar.

::: {.feedback-criteria}
Preserve the German meaning, tense, relationships and sequence
of events. Accept valid alternative Norwegian Bokmål translations. Explain up to
three improvements in English, quoting German or Norwegian where useful. Do not
supply a complete replacement translation.
:::

::: {.feedback-starter}
På lørdag reiser Lena med tog til Oslo. Hun besøker søsteren sin, som bor der
siden to år. Hvis været er fint, går de en tur ved havnen på ettermiddagen.
På søndag reiser Lena hjem igjen.
:::
:::

### 7. Mathematics

::: {.callout-note .example-author-notes}
### For course authors

**Feature:** math-exercise checking alongside the other extensions. Its current
Feedback button still uses legacy settings and the four-step teaching policy;
the shared adapter will be validated here before propagation.
:::

**Your task**

Enter the numerical answer and choose **Check**.

```{math-exercise}
#| label: integration-math
#| context: none
What is $6\cdot7$? _[42]
```


## Python {#python}

Each exercise starts with a partially completed function. Read **Your task**,
finish the code in the editor, then choose **Check** (or press **Shift+Enter**).
You can check your work more than once. **Reset** restores the original draft
and clears the results.

::: {.callout-note .example-author-notes}
### For course authors

These examples use the selected py-exercise revision inside ai-feedback's
integration workbench. Each starter runs but needs a small correction or addition;
none is a complete solution. The tests exercise ordinary cases and edge cases.
The learner's code runs locally in the browser. This page currently demonstrates
the existing checker; the shared AI Feedback adapter will be developed here
before a consumer PR is opened. [Source revisions](resolved-repos.json).
:::

### 1. A price including tax

::: {.callout-note .example-author-notes}
### For course authors

**Feature:** returning a calculated value. The draft already calculates the tax
but returns the untaxed price. Checks include a zero rate and a decimal price.
:::

::: {.example-learner-task #task-price}
**Your task**

Complete `price_with_tax(price, rate)` so that it returns the price plus tax.
The rate is a decimal: `0.25` means 25%, so a price of `100` becomes `125`.
Keep the existing tax calculation and finish the return value. Do not round it.

```{py-exercise}
#| label: practice-price
def price_with_tax(price, rate):
    tax = price * rate
    return price  # TODO: include the tax

## TESTS ##
assert price_with_tax(100, 0.25) == 125, "100 plus 25% tax should be 125"
assert price_with_tax(80, 0) == 80, "A zero rate should leave the price unchanged"
assert abs(price_with_tax(19.5, 0.2) - 23.4) < 1e-9, "Check a decimal price and rate"
```
:::

### 2. Add values with a loop

::: {.callout-note .example-author-notes}
### For course authors

**Feature:** an accumulator and a per-exercise restriction on `sum`.
The starter includes the loop and return; the update inside the loop is unfinished.
The empty-list check already passes, so learners can see partial progress.
:::

::: {.example-learner-task #task-total}
**Your task**

Complete `total(values)` so that it returns the sum of a list of numbers.
Use the existing loop to update `result`; do not use the built-in `sum()`.
An empty list should return `0`.

```{py-exercise}
#| label: practice-total
#| forbidden-keywords: sum
def total(values):
    result = 0
    for value in values:
        result += 0  # TODO: add the current value
    return result

## TESTS ##
assert total([1, 2, 3]) == 6, "The total of 1, 2 and 3 should be 6"
assert total([]) == 0, "An empty list should total 0"
assert total([-4, 1, 2]) == -1, "Include negative values in the total"
```
:::

### 3. Print and return a greeting

::: {.callout-note .example-author-notes}
### For course authors

**Feature:** the difference between printed output and a returned value.
The supplied call displays output even before the missing return is completed.
Checks verify both the return value and the printed greeting.
:::

::: {.example-learner-task #task-greet}
**Your task**

Complete `greet(name)` so that it prints **and returns** `Hello, <name>!`.
The message and `print` line are already there; add the missing return.
Keep the example call below the function to see the output when you choose **Check**.

```{py-exercise}
#| label: practice-greet
def greet(name):
    message = f"Hello, {name}!"
    print(message)
    # TODO: return the message too

greet("Ada")

## TESTS ##
assert greet("Ada") == "Hello, Ada!", "Return the greeting as well as printing it"
assert greet("Lin") == "Hello, Lin!", "Use the name supplied to the function"
with __import__("contextlib").redirect_stdout(__import__("io").StringIO()) as output:
    greet("Mia")
    assert output.getvalue() == "Hello, Mia!\n", "Print exactly one greeting line"
```
:::

### 4. The area of a circle

::: {.callout-note .example-author-notes}
### For course authors

**Feature:** a per-exercise import restriction. The draft has the constant and a
return expression, but is missing a factor of the radius. Checks use a tolerance
for the supplied approximation of pi. Importing `math` is rejected before execution.
:::

::: {.example-learner-task #task-circle}
**Your task**

Complete `circle_area(radius)` using the formula $A = \pi r^2$ for a nonnegative
radius. Use the supplied value `3.14159` for pi; do not import `math`.
Correct the return expression. A radius of zero should give an area of zero.

```{py-exercise}
#| label: practice-circle
#| forbidden-imports: math
def circle_area(radius):
    pi = 3.14159
    return pi * radius  # TODO: apply the area formula

## TESTS ##
assert abs(circle_area(1) - 3.14159) < 1e-6, "Check the area for radius 1"
assert abs(circle_area(2) - 12.56636) < 1e-6, "The radius must be squared"
assert circle_area(0) == 0, "Zero radius should give zero area"
```
:::

### 5. A case-insensitive palindrome

::: {.callout-note .example-author-notes}
### For course authors

**Feature:** `show-test-hints: false` displays check results without assertion
messages. The draft already normalizes letter case. The task explicitly states
the empty-string and punctuation behavior instead of leaving hidden requirements.
:::

::: {.example-learner-task #task-palindrome}
**Your task**

Complete `is_palindrome(text)` so that it returns `True` when the text reads the
same forwards and backwards, ignoring letter case. For example, `"Madam"` is a
palindrome and `"hello"` is not. Keep spaces and punctuation; only letter case is
ignored. The empty string counts as a palindrome.

The lowercase version is ready. Fix the comparison with its reversed version.
This exercise shows which checks pass without detailed hints.

```{py-exercise}
#| label: practice-palindrome
#| show-test-hints: false
def is_palindrome(text):
    normalized = text.lower()
    return normalized == text  # TODO: compare with the reversed text

## TESTS ##
assert is_palindrome("racecar") is True, "Check a lowercase palindrome"
assert is_palindrome("Madam") is True, "Ignore differences in letter case"
assert is_palindrome("hello") is False, "Not every lowercase word is a palindrome"
assert is_palindrome("") is True, "The empty string is a palindrome"
assert is_palindrome("a!") is False, "Keep punctuation in the comparison"
```
:::

### 6. Addition

::: {.callout-note .example-author-notes}
### For course authors

**Feature:** a py-exercise cell with its existing checker and Reset behavior.
This is the first consumer to gain a shared-feedback adapter. That adapter will
be developed and tested on this page before a consumer PR is opened.
This baseline has no AI Feedback button yet.
:::

**Your task**

Implement addition, then choose **Check**. The starter deliberately subtracts.
Use **Reset** to restore the starter.

```{py-exercise}
#| label: integration-add
#| forbidden-imports: os
#| show-test-hints: true
def add(a, b):
    return a - b

## TESTS ##
assert add(1, 2) == 3, "add(1, 2) should return 3"
assert add(0, 0) == 0, "add(0, 0) should return 0"
assert add(-1, 1) == 0, "add(-1, 1) should return 0"
```

### 7. Interactive Python

::: {.callout-note .example-author-notes}
### For course authors

**Feature:** pyodide-interaktiv running on the same page as the checkers and
shared feedback. Its legacy three-stage feedback remains in place until the
shared adapter is developed and tested here.
:::

**Your task**

Run the code and check that the output is **6**. Change the numbers and run it
again, or use **Reset** to restore the original code.

```{pyodide-python}
#| label: integration-python
values = [1, 2, 3]
print(sum(values))
```


::::::

## Integration development

::: {.callout-note .example-author-notes}
### For course authors

The mathematics and interactive Python examples load the selected consumer branches on this same examples
page. They establish a working baseline before the shared feedback adapters are
added here. [Exact source revisions for this build](resolved-repos.json).
:::



::: {.callout-note .example-author-notes}
### For course authors

All shared-feedback development and acceptance checks live in this repository's
single `examples.qmd` file. Add a new tab here for each subsequent integration.
Once an adapter works here, its changes can be propagated in a
separate consumer PR. The order is py-exercise, Pyodide, then mathematics.

For installation and copyable authoring examples, see the
[README and API guide](https://github.com/Erasmus-CTM/ai-feedback#api-quick-start).
:::
